This directory contains example of data. The ```metadata.json``` file 
specify translation from URL to file name. This translation must be
followed in all directories that stores information about datasets.
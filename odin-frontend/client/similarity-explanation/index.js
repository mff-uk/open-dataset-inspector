import { register } from "../app/register.ts";
import explorationComponent from "./similarity-explanation.vue";

register({
  "path": "/similarity-explanation",
  "name": "similarity-explanation",
  "component": explorationComponent,
});
